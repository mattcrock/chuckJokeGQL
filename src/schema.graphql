# Joke defenition
type Joke {
    icon_url: String
    id: String
    url: String
    value: String
  }

# gql Queries
  type Query {
    randomJoke: Joke
    categories: [String]
    jokeByCategory(category:String): Joke
  }
